# 모두약속 — 백엔드 개발 계획

`docs/기술설계_2026-08-06.md`, `docs/API명세서_2026-08-06.md` 기준. 우선순위는 `docs/기능명세서_2026-08-05.md`의 중요도를 따름.

**완료여부**: ✅ 완료 / ⬜ 예정 · **우선순위**: 🔴 높음 / 🟡 중간 / - 해당없음

| 개발 사항 | 완료여부 | 완료 일시 | 우선순위 | 비고 |
|---|---|---|---|---|
| 프로젝트 스캐폴딩 (FastAPI 앱, SQLModel 모델, `/health`) | ✅ | 2026-08-06 | 🔴 | User/LLMCredential/ScheduleSession/FeedbackMessage/ShareLink |
| 개발 도구 (ruff, pytest, pre-commit) | ✅ | 2026-08-06 | 🟡 | `requirements-dev.txt` |
| Alembic 마이그레이션 도입 | ✅ | 2026-08-06 | 🔴 | 초기 스키마 마이그레이션 적용, 앱 기동 시 자동 `create_all` 제거 |
| Google 로그인 (`POST /auth/google`, `GET /me`) | ✅ | 2026-08-06 | 🔴 | TDD로 구현 (`tests/test_auth.py`), `google-auth`로 id_token 검증 |
| API 키 등록·조회·삭제 (`POST`/`GET`/`DELETE /me/llm-credential`) | ✅ | 2026-08-12 | 🔴 | Fernet 암호화 + 제공자별(Claude/GPT/Solar) 접두사 정규식 검증. 저장(`POST`) 시 `ping_provider`로 1회 테스트해 성공해야만 저장, 실패 시 400(2026-08-12 추가) |
| API 키 실동작 테스트 (`POST /me/llm-credential/test`) | ✅ | 2026-08-07 | 🟡 | provider에 "안녕" 보내 응답 확인, 성공 시 `verified_at` 갱신 (`services/llm_ping.py`) |
| Swagger 설정 (앱 설명, 라우터별 태그) | ✅ | 2026-08-07 | - | `/docs`에서 라우터가 헬스체크/인증/API 키로 그룹핑됨 |
| AI 파이프라인 모델 티어 설정 (`pipeline/models.py`) | ✅ | 2026-08-12 | 🔴 | provider×LOW/MID/HIGH → 모델 ID. 3 provider 가격대를 맞춰 LOW/MID/HIGH 재설계(openai: gpt-5-mini/gpt-5.4/gpt-5.5, upstage: solar-pro 고정) — openai/upstage 모델 ID는 가격만 실측, 실제 구조화 출력 호출(`structured_llm.call_structured`)은 스크립트로 별도 검증 필요 |
| AI 파이프라인 스키마·단계 스캐폴딩 (`pipeline/schemas.py` 등) | ✅ | 2026-08-07 | 🔴 | Step1~4 Pydantic 스키마 + 함수 시그니처. LLM 호출부는 `NotImplementedError` |
| Provider별 structured output 공용 인터페이스 (`services/structured_llm.py`) | ✅ | 2026-08-07 | 🔴 | Claude=tool use, GPT/Solar=`.parse()` 공용 (Solar가 이 방식 지원하는 것 실키로 확인) — 2갈래 분기 |
| AI 파이프라인 Step1 — 조건 정규화 (`normalize_conditions`) | ✅ | 2026-08-07 | 🔴 | MID 티어(LOW에서 격상 — 스키마 복잡화 후 LOW가 할루시네이션 실측돼 변경). liked_tags/disliked_tags를 PreferenceTag(verifiable 포함)로 변경, RTF+few-shot 프롬프트, 실제 Solar 키로 end-to-end 확인 |
| AI 파이프라인 Step2 — 후보 생성 Fan-out (`generate_candidates`) | ✅ | 2026-08-09 | 🔴 | HIGH 티어(MID에서 격상). 관점 3개(가성비/동선최소화/취향반영) 병렬 호출, `concurrent.futures`로 개별 타임아웃(180초)+부분실패 허용(`asyncio.wait_for`는 DeepEval eval 테스트의 nest_asyncio 패치와 충돌해 배제, `AI파이프라인_Step별_설계` 참고). "장소 선택"(LLM)과 "시간 배정"(`_schedule_places()`, 결정론적 계산)을 분리해 시간 겹침을 계산 구조상 원천 차단. place_candidates 소싱용 `services/naver_local_search.py` 신규 구현(NAVER API HUB로 이관된 지역검색 API, `naverapihub.apigw.ntruss.com` + `X-NCP-APIGW-API-KEY-ID/KEY` 헤더 — 레거시 개발자센터 엔드포인트와 다름, 실측으로 확인) |
| 여러 지역 검색 병합 (`search_places_for_regions`) | ✅ | 2026-08-09 | 🟡 | `naver_local_search.py`. regions(최대 3개, 프런트가 검증) × 카테고리(맛집/카페/액티비티/문화시설)로 팬아웃 호출 후 title 기준 중복 제거. `POST /schedules` 라우터(`app/routers/schedule.py`, 2026-08-10)가 이 함수를 부른다 |
| Step2 — 좌표 기반 활동 버퍼 추정 (`pipeline/travel_estimate.py`) | ✅ | 2026-08-10 | 🔴 | `_schedule_places()`의 고정 30분 버퍼를 place_candidates 좌표(mapx/mapy, WGS84×1e7로 실측 확인 — 변환 불필요) 기반 직선거리 추정(`estimate_buffer_minutes`, 도로 왜곡 보정×1.2 안전마진)으로 교체. 좌표를 못 찾으면(환각 장소 등) 기존 고정값 폴백 — 기존 유닛테스트 21개 무변경 통과. `reconcile_schedule()`은 Step4가 실제 이동시간을 알아낸 뒤 이후 활동 시간을 보정하는 데 씀(초과분은 무조건 보정, 60분 넘는 여유만 당김) |
| AI 파이프라인 Step3 — 검증·병합 (`synthesize_and_validate`, `synthesize_step3.py`) | ✅ | 2026-08-10 | 🔴 | HIGH 티어. 시그니처에 `conditions: NormalizedConditions` 추가(스텁엔 없었는데 예산/시간 재검증에 필요). 규칙 기반 사전 필터(장소 환각=`ActivityDraft.lat is None`, 시간 겹침 방어적 재확인 — 둘 다 하드 드롭 / 예산 초과 20%·시간 초과 60분 넘으면 하드 드롭, 그 이내는 경고만) + LLM 1회 호출(verifiable=true 태그 위반 추가 검증, why_recommended 생성, 유사도 0.5 이상 쌍은 프롬프트에 얹어 차별점 강조 지시). `Activity`에 `map_url`(naver_map_url) 필드 추가. 유닛테스트 23개, `tests/eval/` 골든셋 4/4 통과(👇 항목) |
| 관점별 재생성 오케스트레이터 (`pipeline/orchestrate.py::generate_schedule_candidates`) | ✅ | 2026-08-10 | 🔴 | Step3가 하드 위반으로 드롭한 후보가 있으면, 그 후보를 만든 관점만 `generate_single_candidate()`로 다시 생성해 Step3를 한 번 더 돌림(관점별 최대 1회). Step3 자체엔 손 안 댐(순수하게 유지) — 재시도는 Step1/2/4처럼 다른 Step을 안 부르는 기존 관례를 따라 별도 오케스트레이터로 뺌. "어느 관점이 빠졌는지"는 스키마에 필드 추가 없이 `Candidate.title`을 원본 `CandidateDraft.title`과 대조해서 알아냄(Step3가 title을 그대로 복사하므로 신뢰 가능). `generate_candidates()`는 하위호환 유지(관점 라벨 없는 얇은 래퍼로 변경, 기존 테스트 무변경 통과) — 관점 라벨이 필요한 새 호출부는 `generate_candidates_with_perspectives()` 사용. 유닛테스트 5개(오케스트레이터) + 5개(Step2 신규 함수) |
| AI 파이프라인 Step4 — 이동 동선 보강 (`enrich_routes`, `enrich_step4.py`) | ✅ | 2026-08-10 | 🔴 | LLM 안 씀. 길찾기 프로바이더는 네이버(신규가입 차단)→카카오모빌리티(대중교통 미지원 확인)→ODsay로 정정 확정, `lab.odsay.com` 학생 등급 키 발급(서비스 플랫폼 URI 방식 — Render 아웃바운드 IP가 단일 고정이 아니라 Server 방식은 불가). `services/odsay_directions.py`: 도보는 좌표 추정(API 호출 없음), 대중교통은 ODsay `searchPubTransPathT`(시간 파라미터 미지원 실측 확인 — "막차 확인" 기능 포기, operating_hours와 같은 자기확인 유도 패턴으로 처리). 700m 이내는 -98 에러로 실측 확인해 도보만 제공. 차량(car) 모드는 프로바이더 미정으로 보류(카카오모빌리티 후보) — 스키마는 확장 가능하게 유지. **파이프라인 순서를 Step1→2→3→사용자 선택→4로 재설계**(2026-08-10, 파일명도 enrich_step3.py에서 변경) — 사용자가 3개 후보 중 고른 1개에만 이 단계를 실행해 ODsay Basic(일 1,000건) 낭비를 줄임. `docs/AI파이프라인_Step별_설계_2026-08-09.md` Step4 절 참고 |
| AI 파이프라인 성능평가 인프라 (DeepEval, `tests/eval/`) | ✅ | 2026-08-07 | 🟡 | `pytest -m eval`로 기본 실행과 분리. `DEEPEVAL_UPSTAGE→OPENAI→ANTHROPIC_API_KEY` 순 자동 폴백(`conftest.py`). Step1용 골든 9케이스 + GEval 채점 완료 — 이 인프라로 Step1 LOW→MID 티어 변경 필요성을 실측으로 발견함 |
| AI 파이프라인 성능평가 — Step2 골든셋 | ✅ | 2026-08-09 | 🟡 | 골든 5케이스(환각 방지/verifiable 하드·소프트/예산/다중지역) + GEval, 실제 Upstage 키로 **5/5 통과(전부 0.80)**. place_candidates 3~4개뿐이라 관점 3개가 수렴하는 문제는 8/7/2/6개로 다양화해서 해결, 예산 합산/카테고리 뒤섞임/시간 겹침 문제는 "장소 선택(LLM)·시간 배정(코드) 분리"로 근본 해결(아래 항목들 참고) |
| Step2 미해결 이슈 — activities 가격대 합이 budget_per_person 초과 가능 | ✅ | 2026-08-09 | 🟡 | `_ROLE_TASK`에 "하한 합 계산해서 넘으면 다시 구성해라" 지시 추가 + Step3(검증·병합) 설계에서 예산 초과를 관대(120% 이내는 정상)하게 다루기로 결정 — 두 조치로 골든셋 재통과 |
| Step2 미해결 이슈 — Solar가 activity의 name/category를 다른 장소 것과 뒤섞음 | ✅ | 2026-08-09 | 🟡 | 프롬프트로는 해결 안 돼서(모델 신뢰성 문제) `_correct_categories()`로 place_candidates 기준 category를 결정론적으로 재보정 — LLM이 만든 category는 아예 안 믿는 방식으로 근본 해결 |
| AI 파이프라인 성능평가 — Step3 골든셋 | ✅ | 2026-08-10 | 🟡 | 골든 4케이스(disliked verifiable=true 하드 드롭, verifiable=false 소프트 신호 hedge, 유사 후보 차별화, 전부 위반→InfeasibleResponse) + GEval, 실제 Upstage 키로 **4/4 통과(0.80~1.00)**. 규칙 기반 필터(장소 환각·시간 겹침·예산/시간 대폭 초과)로 이미 걸러지는 것들은 유닛테스트로 충분해서 제외하고, LLM만 판단 가능한 것에 집중. 최초 실행에서 "전부 드롭→InfeasibleResponse" 케이스가 0.40으로 실패했는데, 실제 동작은 맞았고 judge가 "kept 0개라 평가할 게 없다"를 미흡함으로 오판한 것 — criteria에 "kept=0이면 관련 기준은 자동 통과"를 명시해서 1.00으로 해결(Step2 eval에서 겪은 "criteria 애매성" 패턴 재현) |
| AI 파이프라인 성능평가 — Step4용 골든셋 | - | | - | Step4는 LLM을 안 써서(ODsay 호출만) DeepEval 적용 대상 아님 — 유닛테스트(mock)로 충분 |
| Step4 자차 옵션 (`services/naver_directions.py::get_car_option`) | ✅ | 2026-08-10 | 🔴 | NCP Maps Directions 5. 콘솔에서 신규 이용 신청이 실제로 열려 있는 걸 확인해 "프로바이더 미정으로 보류"였던 걸 해소. `enrich_step4.py`에 도보·대중교통과 나란히 연결, 프로바이더별 독립 실패 처리. 유닛테스트(mock) + 실제 키 호출 둘 다 확인 완료 — 처음엔 errorCode 210 "구독 필요"가 떠서 NCP 공식 문의 접수했는데, 콘솔의 "Maps"(VPC) Application은 레거시 `naveropenapi.apigw.ntruss.com`이 아니라 `maps.apigw.ntruss.com`을 써야 한다는 답변을 받고 도메인 수정해 해결(여러 블로그·문서가 레거시 도메인만 보여줘서 실측 없이 따라간 게 원인) |
| `enrich_routes()` 입출력을 `CandidateDraft`→`Candidate`로 정리, `EnrichedCandidate` 삭제 | ✅ | 2026-08-10 | 🔴 | 라우터 구현 중 발견 — 실제로 사용자가 고르고 DB에 저장되는 건 Step3가 만든 `Candidate`인데 `enrich_routes()`가 `CandidateDraft`를 받아 별도 타입(`EnrichedCandidate`)을 반환해서 라우터가 왕복 변환을 떠안아야 했음. `Activity`에 `lat`/`lng` 추가(Step4가 좌표로 경로 조회하려면 필요) |
| 일정 생성 API (`POST /schedules`) | ✅ | 2026-08-10 | 🔴 | Step1→Step2→Step3 연결(`orchestrate.generate_schedule_candidates`), 422(요청 검증, FastAPI 기본)/409(`InfeasibleResponse`, `JSONResponse`로 직접 반환해 API명세서 예시와 같은 평평한 구조 유지)/502(장소 검색 실패) 처리. `ScheduleSession`은 파이프라인 성공 이후에만 생성 — 실패 케이스마다 롤백 코드 안 써도 됨 |
| 후보 선택 후 경로 조회 API (`POST /schedules/{id}/routes`) | ✅ | 2026-08-10 | 🔴 | 사용자가 3개 중 1개를 고르면 그 후보에 한해 `enrich_routes()` 호출, 결과를 `ScheduleSession.candidates`에 반영해 재저장. Step4는 LLM을 안 써서 BYOK 크리덴셜 조회 없음 |
| 일정 조회 API (`GET /schedules/{id}`) | ✅ | 2026-08-10 | 🟡 | 본인 소유 확인 (403), 존재하지 않으면 404 |
| 필수 장소 선택·재생성 (`POST`/`DELETE /schedules/{id}/required-places`, `POST /schedules/{id}/regenerate`) | ✅ | 2026-08-12 | 🔴 | 후보 풀에서 고른 장소를 `ScheduleRequiredPlace`에 영속화하고, `ScheduleSession.normalized_conditions`와 기존 `SchedulePlacePool`만 재사용해 검색/Step1 없이 재생성. Step2는 필수 장소 중심 생활권만 만들고 Step3는 `place_id`가 실제 활동에 전부 포함되지 않으면 하드 드롭하므로 후보 풀에 넣기만 하고 빠지는 일을 막음. 실패 시 기존 후보는 보존하고 409 사유만 반환 |
| `schedule_session.status` DB CHECK 제약 (`draft`/`confirmed`만 허용) | ✅ | 2026-08-07 | 🟡 | 마이그레이션 `f4f8459f626b`. "이미 confirmed면 재확정 불가" 같은 전이 규칙은 라우터 로직 몫 — 아래 항목에서 구현 |
| 일정 확정 (`POST /schedules/{id}/confirm`) | ✅ | 2026-08-10 | 🟡 | status draft→confirmed 갱신, `confirmed_candidate_id` 기록. 이미 confirmed인 세션 재확정 시 409 |
| 공유 링크 생성 (confirm과 통합) | ✅ | 2026-08-10 | 🟡 | 별도 `POST /share` 엔드포인트 없이 confirm 응답에 `share_slug`(8자 base62)를 바로 실어 보냄 — Task 12(프런트 ShareView)가 추가 호출 없이 그대로 씀 |
| 공유 링크 공개 조회 (`GET /share/{slug}`) | ✅ | 2026-08-10 | 🟡 | 인증 불필요, slug로 확정된 일정 조회. `schedule._find_candidate()` 재사용, 응답은 기존 `Candidate` 스키마 그대로(새 응답 모델 안 만듦) |
| 배포 설정 (Render) | ✅ | 2026-08-10 | - | `moduyaksok.onrender.com` (사용자 확인, 2026-08-11 — 이 표가 못 따라가고 있었음) |
| 태그 매칭 정밀도 개선 — verifiable liked/disliked 태그 전용 검색 | ✅ | 2026-08-11 | 🔴 | 2026-08-10 미해결 설계 질문 (b) 해소. `naver_local_search.search_places_for_regions()`가 verifiable=true 태그마다 `"{region} {tag}"` 검색을 추가 호출 — liked 매칭 장소는 `matched_tag` 부착(카테고리 검색에서만 나온 place와 구분), disliked 매칭 장소는 결과에서 아예 제거(LLM이 볼 수도 없게). 기존 category/title 텍스트만 보고 LLM이 사후 추측하던 방식보다 강한 근거 |
| 태그 중복 반영 방지 — 후보당 태그 1곳 하드룰 | ✅ | 2026-08-11 | 🟡 | 2026-08-10 미해결 설계 질문 (a) 해소. `generate_step2._ROLE_TASK`에 "같은 태그는 후보당 최대 1곳" 지시 추가 + `synthesize_step3._has_duplicate_tag_match()`가 `ActivityDraft.matched_tag` 기준으로 하드 드롭(프롬프트만 믿지 않고 결정론적으로 한 번 더 강제) |
| verifiable 태그 개수 상한 (좋아하는/싫어하는 것 각 최대 3개) | ✅ | 2026-08-11 | 🟡 | 태그 검색이 지역 확장과 곱해져 호출량이 커지는 걸 제어. `normalize_step1.py` 프롬프트가 중요도 순으로 3개만 남기도록 지시(1차), `NormalizedConditions.cap_verifiable_tags` validator가 방어용 하한선(2차) — 초과해도 요청을 실패시키지 않고 조용히 자름(LLM 품질 문제지 사용자가 검증을 우회하려는 상황이 아니라서) |
| 세부지역 없는 광역 시/도 자동 확장 (`app/services/regions.py`) | ✅ | 2026-08-11 | 🟡 | "서울"처럼 세부지역 없이 시/도만 입력하면 통짜로 검색하던 걸, `expand_broad_region()`으로 세부지역들("서울 강남", "서울 홍대" 등)로 펼쳐서 각각 독립 검색하도록 변경 — `moduyaksok-frontend/src/lib/regions.ts`의 REGIONS를 백엔드에도 복제(프런트 TS를 백엔드가 import 못 해서). 서울에 용산 추가 |
| 이동거리 하드룰 — 후보 내 활동 간 과도한 이동 방지 | ✅ | 2026-08-11 | 🟡 | 여러 지역을 입력했을 때 서로 먼 지역끼리(예: 서울 강남+경기 수원) 하루 코스로 섞이거나, 광역 확장으로 생긴 세부지역끼리도 먼 곳이 섞이는 문제 대응. `synthesize_step3._has_excessive_travel()`이 `travel_estimate.estimate_buffer_minutes()`로 활동 간 추정 이동시간을 계산해 임계값(초기값 60분, `_MAX_TRAVEL_MINUTES`) 넘으면 하드 드롭 — 입력 지역이 몇 개인지 추적할 필요 없이 좌표만으로 판단. 임계값은 실측 후 조정 필요 |
| 네이버 지역검색 호출량 제어 (rate limiter + 일일 예산) | ✅ | 2026-08-11 | 🔴 | 태그 검색·광역 확장으로 호출량이 네이버 API HUB 한도(초당 10건, 일일 25,000건)를 크게 넘길 수 있어 `app/services/rate_limiter.py` 신규. 초당 상한은 세션(=`POST /schedules` 요청 하나) 단위 라운드로빈 토큰버킷(`_RoundRobinLimiter`) — 세션이 하나뿐이면 기존 단순 토큰버킷과 동일하게(버스트 포함) 동작하고, 여러 세션이 동시에 콜을 쏟아낼 때만 라운드로빈으로 나눠 갖게 해서 한쪽이 다른 쪽을 오래 굶기지 않는다(처음엔 순수 FIFO로 시작했다가 이 문제 지적을 받고 같은 날 업그레이드). 일일 상한은 Redis(`REDIS_URL`, `moduyaksok-db` docker-compose에 `redis` 서비스 추가) `INCRBY`+TTL로 여러 워커/인스턴스에서도 정확히 집계. 예산 부족 시 요청을 막지 않고 확보되는 만큼만 검색해 place_candidates가 적은 채로 진행(2026-08-11 결정) |
| ODsay 일일 호출량 제어 (Step4) | ✅ | 2026-08-13 | 🟡 | ODsay Basic 무료 등급 일일 한도(1,000건)를 지금까지 코드가 안 지키고 있었음(3개 후보 중 1개만 호출하는 설계로 아끼기만 했지 하드 상한은 없었음) — `rate_limiter.reserve_daily_budget()`을 네이버 전용에서 `(resource, requested, daily_limit)` 범용으로 일반화해 `enrich_step4.py`가 구간마다 ODsay 호출 전에 `"odsay"` 리소스로 예산 1개씩 확보하게 함(네이버와 독립된 Redis 카운터). 예산 소진 시 그 구간은 ODsay 호출 자체를 스킵하고 도보 옵션만 남긴 채 경고 문구 추가(요청 실패시키지 않음, 기존 degrade 정책과 동일). `ODSAY_DAILY_CALL_LIMIT`(기본 1000) 신규 환경변수. 참고: 네이버의 월 775,000건 한도는 25,000×31과 정확히 일치해 일일 한도만 지키면 자동 보장되므로 별도 월간 카운터는 안 만듦 |
| purpose(목적)별 Step2 프롬프트 가이던스 | ✅ | 2026-08-11 | 🟡 | 지금까지 `purpose`가 프롬프트에 원문 그대로만 들어가고 어떻게 반영하라는 지시가 없었음(검증된 적 없는 신호, 사용자가 지적). `generate_step2._PURPOSE_GUIDANCE`로 date/friends/family/party 각각 구체 지시문 추가, other는 무난하게(별도 지시 없음) — 프런트에 "기타" 옵션이 없던 것도 같이 추가 |
| 관점별 태그 매칭 장소 분배 — 후보 3개 유사도 문제 | ✅ | 2026-08-11 | 🔴 | 사용자 관측: "와플" 검색 시 3개 관점이 거의 같은 장소로 수렴 — `_ROLE_TASK`가 verifiable 태그 반영을 관점 구분 없이 공통 지시해서, 강한 태그가 있으면 3개 관점 다 같은 상위 매칭을 욕심냄. `generate_step2._tag_bundles_by_perspective()`로 해결 — 관점마다 다른 태그 매칭 장소를 "시드"로 배정하고, 다른 태그는 좌표(mapx/mapy, Step4 이동시간 추정과 같은 값)상 그 시드와 가장 가까운 매칭으로 묶어서 관점별로 다른 place_candidates 부분집합을 준다(매칭 안 된 카테고리 장소는 그대로 공유). 같은 곳을 여러 관점이 아예 볼 수조차 없게 만드는 방식이라 프롬프트 지시보다 강함. `generate_single_candidate()`(관점 재생성)도 같은 파티션을 다시 계산해서 씀 |
| 식사 슬롯(점심/저녁) 보장 하드룰 | ✅ | 2026-08-11 | 🔴 | 사용자 관측: 강한 liked_tag("와플")가 있으면 디저트/카페만으로 일정이 채워지고 점심·저녁이 아예 없는 경우가 있었음. `naver_local_search.py`가 카테고리 검색 버킷을 `source_category`로 place dict에 부착(matched_tag와 같은 패턴) → `ActivityDraft.source_category`까지 전파. time_range가 점심(12~13시)/저녁(18~19시)을 포함하면 그 시간대에 식사류 카테고리 장소 최소 1곳을 유저 프롬프트에 동적 지시(1차, `generate_step2._meal_slot_instruction`) + `synthesize_step3._has_missing_meal_slot()`이 그 시간대와 겹치는 식사류 활동이 없으면 하드 드롭(2차, 결정론적 강제). 정확히 그 시각에 먹으라는 게 아니라 "그 시간대가 껴 있으면 식사 하나는 넣는다"는 느슨한 기준 — 나중에 피드백 단계에서 "점심은 만나서 먹을 거예요" 같은 입력이 이 요구사항을 덮어쓰게 확장 가능. `_format_place_candidates()`도 place 목록에 카테고리 버킷 라벨을 표시해서 LLM이 텍스트로 카테고리를 추측하지 않게 함 |
| 지역 범위 축소(단일 세부지역 필수) + 카테고리 세분화 + verifiable 태그 상한 5 | ✅ | 2026-08-11 | 🔴 | 사용자가 NAVER API HUB 공식 문서로 `display`(최대 5)/`start`(사실상 고정) 제약을 재확인 — 여러 지역 조합 지원을 포기하고 `NormalizedConditions.regions: list[str]` → `region: str`(세부지역 필수, `validate_region`)로 축소. `app/services/regions.py`(광역 시/도 자동 확장)는 쓸 곳이 없어져 삭제. `naver_local_search._PLACE_CATEGORIES`를 4개→15개(한식/중식/일식/양식/분식/고깃집/카페/베이커리/술집/액티비티/방탈출/보드게임카페/전시/공연장/영화관)로 세분화해 카테고리별 `display` 상한(5)의 실효 커버리지를 늘림 — 식사 슬롯 하드룰은 `_MEAL_CATEGORIES` 집합 기준으로 대체. `MAX_VERIFIABLE_TAGS` 3→5(호출량 여유가 생겨 상향), `search_places_for_regions()` → `search_places_for_region()` 개명. 프런트 `ConditionWizardView.vue`도 다중 지역 입력 UI(지역 추가/삭제, 시/도만 선택 가능)를 시/도+세부지역 단일 필수 선택으로 교체 |
| `SchedulePlacePool` 테이블 — 장소 풀 영속화 | ✅ | 2026-08-11 | 🔴 | 나중에 피드백으로 일정을 수정할 때마다 네이버 API를 다시 안 부르기 위한 사전 준비(피드백 엔드포인트 자체는 아직 미구현, 이번엔 저장만 구현). 검색된 place_candidates·검색된 verifiable 태그를 `schedule_session`과 1:1(FK unique)로 연결된 별도 테이블에 저장 — `schedule_session`에 컬럼으로 얹는 대신 분리한 이유는 두 데이터의 갱신 빈도·읽히는 이유가 달라서(`schedule_session`은 확정 시점에만 바뀌는 핵심 엔티티, `schedule_place_pool`은 피드백마다 갱신될 내부 상태). 마이그레이션 `c24bd64ba972`. `POST /schedules` 라우터가 `ScheduleSession`과 같은 트랜잭션에서 저장(`session.flush()`로 FK 순서 보장) |
| 파이프라인 단계별 개발용 로그 (`[step1]`~`[step4]`) | ✅ | 2026-08-14 | - | 개발 중 콘솔에서 지금 어느 단계인지 보려는 용도(운영 모니터링 아님, API 응답에는 안 실림). `orchestrate.py`가 Step1(조건 정규화·네이버 검색)/Step2(후보 초안)/Step3(검증·병합)를, `routers/schedule.py`의 `create_schedule_routes`가 Step4(경로 보강)를 각각 `logger.info("[stepN] ...")`로 로깅 — "여러 Step을 잇는 건 오케스트레이터/라우터 몫" 관례를 따라 각 Step 파일 내부는 안 건드림. 부수 발견: `logging.basicConfig`가 앱 어디에도 없어서 루트 로거가 기본 WARNING에 머물러 있었고, 그 결과 기존 `structured_llm.call_structured()`의 INFO 로그(provider/모델/토큰 수, 2026-08-12 도입)도 지금까지 콘솔에 실제로는 안 찍히고 있었음 — `main.py`에 `logging.basicConfig(level=logging.INFO)` 추가로 같이 해결(앱 진입점 한 곳만 고치면 모든 `app.*` 로거가 상속받음) |
| 소도시 지역에서 일정 생성이 항상 409로 실패하는 문제 — 클러스터 반경 상한 5km로 확장 | ✅ | 2026-08-14 | 🔴 | 사용자가 "경기 구리"로 실제 테스트하다 발견 — 새로 추가한 `[step1]`~`[step2]` 로그로 Step2가 34ms 만에(소프트 선호도 LLM 호출도 못 가고) "조합 부족"으로 즉시 실패한 걸 확인. `generate_step2._build_candidate_plans()`가 지리적 클러스터를 1km→1.5km→2.5km까지만 넓히는데, 시간대가 점심·저녁을 모두 포함하면(`meal_slots=2`) 클러스터 하나에 식사 카테고리 장소가 최소 2곳 모여야 플랜이 만들어짐(`_plans_for_cluster`) — 상권이 좁고 흩어진 소도시는 2.5km 안에 식사 카테고리 결과가 2곳도 안 모여 플랜 자체가 0개, 바로 `InfeasibleResponse`(409)로 떨어짐. `_CLUSTER_RADIUS_STEPS_METERS`에 `5_000` 단계 추가로 해결 — 반경 확장 루프가 이미 "필요한 만큼만 늘리다 플랜 3개 채우면 멈춘다" 구조라 밀집 지역(강남 등)은 여전히 1km에서 끝나 영향 없음. `tests/test_generate_step2.py`의 "먼 장소는 클러스터 밖" 테스트가 옛 2.5km 경계(약 2.6km 떨어진 장소)를 기준으로 했던 걸 새 5km 경계(약 6.2km)로 재조정 |
| 일정이 먹거리 위주로 쏠리는 문제 — 놀거리 카테고리 최소 1곳 보장 | ✅ | 2026-08-14 | 🔴 | 사용자 관측: 일정이 식사/카페 위주고 전시·액티비티·보드게임카페·공연장·방탈출 같은 놀거리가 잘 안 나옴. `generate_algorithm_step2.py`에 `_ACTIVITY_CATEGORIES`(액티비티/방탈출/보드게임카페/전시/공연장/영화관) 추가 — (1) `_score_place`가 이 카테고리를 처음 담을 때 일반 카테고리 다양성 보너스(+5)에 +4를 더 얹어 beam search가 먼저 담도록 유도(소프트), (2) `_generate_for_plan`의 완성 조건에 "식사 슬롯 외에 자리가 남고(`wanted > meal_slots`) 후보 풀에 이 카테고리가 실제로 있으면 최소 1곳은 포함"을 하드 조건으로 추가. 후보 풀에 해당 카테고리가 아예 없는 지역까지 강제하면 "경기 구리" 사례처럼 조합 자체가 안 나와 409로 떨어지므로, 있을 때만 강제(`activity_available`). `tests/test_generate_algorithm_step2.py`에 놀거리 태그 없이도(순수 음식 태그만) 놀거리 카테고리가 포함되는지 검증하는 테스트 추가 |
| 좋아요 태그가 "방탈출"처럼 고정 카테고리와 겹치면 하트 표시가 안 붙는 문제 — Step1 few-shot 보강 | ✅ | 2026-08-14 | 🔴 | 사용자 관측: 일정에서 "방탈출"을 좋아한다고 적어도 좋아요 그룹에 안 뜨고 하트 표시도 안 붙음. `normalize_conditions()`를 실제 upstage LOW(solar-pro4)로 직접 호출해 재현 — "방탈출"이 `verifiable=false, preference_kind=activity_level`(주관적 활동 취향)로 잘못 분류돼, verifiable=true 태그만 도는 naver_local_search 태그 검색이 아예 안 걸려서 `matched_tag`가 못 붙었음(카테고리 검색으로만 잡혀 `source_category`만 있음). 원인은 `_SYSTEM_PROMPT`의 few-shot 5개가 전부 음식류/분위기 예시뿐이라 방탈출·보드게임카페·전시 같은 "구체적 장소 카테고리형" 선호를 모델이 분류할 근거가 없었던 것 — verifiable=true 설명에 장소 카테고리 예시 추가 + 이 패턴을 직접 겨냥하는 예시 6번(place_type, verifiable=true) 추가로 해결. 재실측으로 확인: "방탈출"뿐 아니라 예시에 없던 "보드게임카페"/"전시"도 verifiable=true로 일반화됐고, 진짜 주관적 취향("시끌벅적한 분위기")은 여전히 verifiable=false로 남아 회귀 없음 |
| 좋아요 태그가 전부 비식사(non-meal)일 때 밀집 지역에서도 409("조합 부족") | ✅ | 2026-08-14 | 🔴 | 사용자가 "서울 홍대"(좋아요: 와플·방탈출, 둘 다 is_meal=False)로 재현 — 위 few-shot 수정 직후라 지리적 반경 문제("경기 구리")는 아닐 것으로 보고, Step1→네이버 검색→`generate_algorithm_candidates`를 실제 API로 그대로 재현+단계별 트레이스: plan은 정상 생성(anchors=와플 카페+방탈출 장소, 5분 거리에 식사 장소 49곳), 그런데 beam search가 5곳을 다 채워도 `meal_count`가 0~1(필수 2 미달)이라 전량 드롭. 원인은 좋아요 태그에 식사 태그가 하나도 없으면(`meal_tags=()`) 식사 앵커가 하나도 강제되지 않아 두 식사 슬롯 다 "자연 경쟁"에 맡겨지는데, 바로 위 항목에서 추가한 활동 카테고리 점수 가산(+4)이 식사 카테고리의 새-카테고리 보너스(+5)와 맞먹어서 beam이 식사 대신 활동 장소로 자리를 채우는 쪽을 더 자주 택함 — `generate_algorithm_step2._score_place()`에 "식사 슬롯이 덜 찼으면(`state.meal_count < meal_slots`) 식사 카테고리에 +6 가산" 추가로 해결(슬롯이 다 차면 가산 사라짐, `_MEAL_CATEGORIES`는 `generate_step2`에서 import). 재현 스크립트로 수정 전/후 확인(0개 → 3개), `tests/test_generate_algorithm_step2.py`에 비식사 좋아요 태그만 있어도 `meal_count>=2`인지 검증하는 회귀 테스트 추가. 부수 발견: Step1이 같은 입력에도 disliked "콩"을 liked_tags에도 중복으로 넣은 사례가 한 번 관측됨(비결정적) — 이번 버그의 원인은 아니었지만 재발하면 살펴볼 것 |
| 후보 3개가 같은 좋아요 앵커(장소)를 거의 그대로 재사용하는 문제 | ✅ | 2026-08-14 | 🔴 | 사용자가 "경기 수원"(좋아요: 방탈출·와플)에서 후보 3개가 전부 "셜록홈즈 수원역점"(방탈출)+"청23 AK 수원"(와플)을 공유하는 걸 스크린샷으로 제보 — 실제 API로 재현: 이 지역엔 방탈출 매칭 장소 6곳, 와플 매칭 장소 9곳이나 있는데도 `_select_candidate_plans()`가 매번 같은 두 앵커를 골랐음. 원인은 `generate_step2._select_candidate_plans()`의 정렬 기준 1순위가 `len(anchor_names)`(앵커 총 개수)였다는 것 — `_tag_anchor_variants`의 "다양화 장소" 보정(실제 태그 매칭이 적은 클러스터에서 앵커 수를 채우려고 무관한 장소를 더 얹는 장치)이 앵커 개수만 부풀려서, 이미 두 앵커를 재사용하면서 다양화 장소만 바꾼 plan이 "총 개수가 더 많다"는 이유로 진짜 다른 앵커 조합(겹침 0)을 가진 plan보다 두 번째·세 번째 픽에서도 계속 이겼음. 정렬 순서를 `len(anchor_names - used_anchor_names)`(새 앵커 수) → `-max_anchor_overlap`(이미 뽑힌 plan과의 앵커 중복 최소화) → `len(anchor_names)` 순으로 재배치 — 첫 픽은 `selected`가 비어 있어 overlap이 항상 0이라 기존 동작 그대로고, 두 번째·세 번째 픽부터 실제 다양성이 생김. 재현 스크립트로 확인: 수정 후 3개 후보가 서로 다른 방탈출/와플 장소(또는 아예 다른 태그 조합)를 씀. `tests/test_generate_step2.py::test_candidate_plans_assign_different_real_meal_anchors_to_each_option`이 "비식사 독립 태그(와플)가 매번 등장해야 한다"는 옛 기대치를 갖고 있었는데, 이제는 의도적으로 최소 1번만 등장하도록 재조정 |
| `GET /confirmed-schedules`가 확정 전 draft를 안 보여주던 문제 | ✅ | 2026-08-14 | 🔴 | 사용자 리포트: 확정하기 전 일정이 목록에 안 보임 — `POST /schedules` 성공 시점에 이미 `ScheduleSession`이 draft로 저장되는데(2026-08-11 항목 참고) 이 목록 엔드포인트만 `status == "confirmed"`로 걸러내고 있었음. 필터를 없애 draft·confirmed 전부 반환하도록 변경, 응답 모델을 `ConfirmedScheduleSummary` → `ScheduleSummary`로 개명하고 `status` 필드 추가(프런트가 이 값으로 초안/확정 UI를 분기). draft의 `candidate_title`은 확정 후보가 없으니 첫 번째 생성 후보 제목으로 채움. 엔드포인트 경로(`/confirmed-schedules`)는 이름과 실제 동작이 안 맞게 됐지만, 호출부(프런트 store·router)까지 전부 바꾸는 범위라 이번엔 유지 — 필요해지면 그때 같이 옮길 것. `tests/test_schedule.py`에 draft+confirmed가 함께 반환되고 각각 `status`/`share_slug`가 맞는지 검증하는 테스트 추가 |
| `DELETE /schedules/{id}`가 draft를 못 지우던 제약 제거 | ✅ | 2026-08-14 | 🟡 | 바로 위 항목으로 "나의 일정" 목록에 draft가 함께 보이게 되자, 사용자가 만들다 만 초안에 삭제 버튼이 없는 걸 바로 지적 — `delete_confirmed_schedule()`(→ `delete_schedule()`로 개명)의 `status != "confirmed"` 409 제약은 애초에 목록이 confirmed만 보여주던 시절(2026-08-13 최초 구현)의 잔재였고, draft를 지울 UI 자체가 없어서 문제가 안 됐을 뿐이었다. 제약을 없애 소유자면 draft/confirmed 상관없이 삭제 가능하게 변경 — draft는 `ShareLink`가 애초에 없어 그 삭제 루프가 자연히 0건 처리됨. `tests/test_schedule.py`에 draft 삭제 성공/confirmed 삭제 회귀/타인 세션 403 테스트 3개 추가(이 엔드포인트는 그동안 테스트가 하나도 없었음) |
| 후보 상세 화면 드래그 순서 변경 시 교통편 재계산 (`POST .../reorder/preview`, `POST .../reorder/save`) | ✅ | 2026-08-14 | 🟡 | 사용자 질문("드래그로 순서 바꾸면 교통편도 즉시 재계산되냐")을 계기로 설계·구현. 장소 제외(removal) preview/save와 같은 구조 — `_candidate_reordered()`가 순서만 재배열한다: 각 활동의 원래 체류시간은 보존하고 시작 시각은 세션 `time_range` 시작부터 gap=0으로 다시 이어붙여, 기존 `enrich_routes()`의 `reconcile_schedule()` 보정이 구간마다 실제 이동시간만큼 자동으로 벌려주게 만들었다(그 함수는 한 줄도 안 고침). `ordered_positions`는 현재 `order` 값 1..n의 중복 없는 순열이어야 하며 아니면 422(`_validate_ordered_positions`). `tests/test_schedule.py`에 순서 재배열·시간 재계산·비순열 거부(422)·타인 세션(403)·저장 후 confirmed→draft 전환 검증 테스트 4개 추가. 설계 문서: `docs/superpowers/specs/2026-08-14-candidate-reorder-transport-design.md` |
| Step3 title이 카테고리를 화살표로 기계적으로 나열하는 문제 — 프롬프트 보강 | ✅ | 2026-08-14 | 🟡 | 사용자 관측: "보드게임 → 돼지고기구이 → 카페 → 체험 → 분식 코스"처럼 title이 부자연스럽게 카테고리 나열이 됨 — `synthesize_step3._ROLE_TASK`의 title 지시가 "장소 목록을 잘 나타내면서..."로만 돼 있어 구체적 스타일이 없었음(Step1 few-shot 보강과 같은 패턴으로, 관측된 실패 사례를 부정 예시로 직접 박고 자연스러운 문구 긍정 예시 추가). eval 골든셋(`tests/eval/test_step3_synthesize_eval.py`)은 실제 LLM 호출이 필요해 이번엔 안 돌림 — 필요하면 `pytest -m eval` |
| 제주 등 자차 기본 지역이 이동거리 하드룰에 걸려 "조건 불만족"이 과도한 문제 — 자차 속도 상수 분리 | ✅ | 2026-08-14 | 🔴 | 사용자 관측: 제주는 장소 간 거리가 멀어 일정 생성이 자주 실패함. 원인은 `travel_estimate.estimate_buffer_minutes()`가 대중교통 체감 속도(18km/h)만 가정해서 `_MAX_TRAVEL_MINUTES`(15분)가 실제로는 직선거리 약 2.9km밖에 안 됐던 것 — 임계값(분)을 올리는 대신 근본 원인(속도 가정)을 고쳤다. `is_car_dependent_region()`(제주·강원부터, 필요시 확장) 신규 — 자차 기본 지역엔 42km/h 상당 속도(`_CAR_SPEED_M_PER_MIN`)로 계산해 같은 15분 임계값이 약 6.7km까지 커버하게 함. `estimate_buffer_minutes()`에 `car_dependent: bool = False` 파라미터 추가(기본값이라 기존 호출부 전부 무변경 동작), `generate_algorithm_step2.py`(beam search 필터·정렬·시간배정)·`generate_step2.py`(`_schedule_places`)·`synthesize_step3.py`(`_has_excessive_travel` 하드룰)까지 일관되게 같은 속도 가정을 쓰도록 스레딩 — Step2가 통과시킨 조합을 Step3가 다른 기준으로 재드롭하는 일이 없게 함. 테스트: `test_travel_estimate.py`(속도/지역 판별)·`test_generate_algorithm_step2.py`(제주 좌표 5km 쌍이 지역에 따라 통과/차단 갈리는지) |
| 안 쓰는 `feedback_message` 테이블 삭제 | ✅ | 2026-08-15 | - | 사용자가 DB에서 이 테이블이 남아 있는 걸 보고 지적 — grep으로 확인해보니 `_delete_schedule_records()`의 정리 대상 목록에만 있고 실제로 row를 insert하는 코드는 어디에도 없는 미구현 기능(자유 텍스트 피드백)의 잔재였음. `FeedbackMessage` 모델·`app/models/__init__.py` export·라우터 import/정리 루프 참조 모두 제거, 마이그레이션 `f1a2b3c4d5e6`(`DROP TABLE feedback_message`)로 실제 DB에도 적용. `docs/ERD_2026-08-06.md`(2.6절 제거, 이후 절 번호 당김)·`docs/기술설계_2026-08-06.md`(스키마 요약 코드블록에서 제거)도 동기화 |
| "일정 추가하기" — 뺀 곳 없이도 항상 AI로 장소 1개 추가 | ✅ | 2026-08-15 | 🟡 | 기존엔 장소를 하나 뺀 뒤에만 "대체 장소 채우기" 버튼이 떴는데, 사용자가 "뺀 것 없이도 항상 상세 화면에 추가 버튼이 보이면 좋겠다"고 요청. `preview_candidate_replacement`(`POST .../candidates/{id}/preview`)가 `excluded_place_ids=[]`도 받도록 검증 완화 — 뺀 곳이 있으면 그 자리를 채우고, 없으면 `_generate_candidate_replacement`의 `replacement_count`를 1로 보정해 장소 1개만 새로 추가한다(요청 개수 그대로 쓰면 0개 채우기가 돼버림). 이미 `_MAX_PLACES`(7곳)까지 찬 상태에서 추가를 시도하면 409로 막음. 후보 병합 로직(`_place_replacements_in_removed_slots`)은 기존에 "제외 없음"(removed_slots=0) 케이스를 이미 지원하고 있어서 그대로 재사용. 프런트는 새 엔드포인트 없이 기존 `previewCandidateReplacement()` 스토어 액션을 빈 배열로도 호출하도록만 바꿈. 테스트: `test_replacement_preview_adds_one_place_without_exclusions`, `test_replacement_preview_rejects_add_when_already_at_max_places` |
| 커스텀 필수 장소가 낀 후보에서 "일정 추가하기"/뺀 자리 채우기가 항상 409로 실패하는 버그 | ✅ | 2026-08-15 | 🔴 | 사용자가 실제 세션 로그(`candidate_replacement_stage ... draft_count=0` → 409)로 리포트: "빼고 넣은건데 못 찾는다"는데, 그 후보엔 이름 검색으로 직접 추가한 필수 장소(`is_custom=True`)가 있었음. 원인 추적: `_generate_candidate_replacement()`가 `available_places`를 `SchedulePlacePool`에서만 구성하는데, is_custom 필수 장소는 애초에 place_pool에 없다(표준 카테고리·태그 검색 결과가 아니므로) — 그런데 그 장소는 `fixed_place_ids`(유지해야 할 장소)엔 들어가서, `generate_algorithm_step2._temporary_clusters()`의 `required_place_ids.issubset(places_by_id)` 검사가 항상 실패해 클러스터가 0개로 떨어졌음(빠르게 실패해서 `elapsed_seconds=0.001`이었던 이유). `regenerate_schedule()`엔 이미 커스텀 장소를 `place_candidates`에 좌표째로 주입하는 로직이 있었지만(2026-08-15 앞선 커스텀 필수 장소 기능 추가 때 구현) `_generate_candidate_replacement()`엔 없었다 — 그 주입 로직을 `_custom_required_place_candidates()` 공유 헬퍼로 빼서 두 곳(그리고 `regenerate_schedule()`의 "후보별 제외" 재생성 분기까지 총 세 곳)에 적용. 회귀 테스트로 먼저 재현 확인(수정 전 실패 → 수정 후 통과) 후 커밋: `test_replacement_preview_injects_custom_required_place_into_place_candidates` |
| 도보 이동시간이 실제보다 훨씬 짧게 표시되는 버그 | ✅ | 2026-08-15 | 🔴 | 사용자 리포트: "도보로 가는 길의 시간 측정이 잘못되고 있는거 같다". 원인: `odsay_directions.get_walk_option()`이 Step2 버퍼 추정용 `travel_estimate.estimate_buffer_minutes()`를 그대로 재사용하고 있었는데, 그 함수는 직선거리가 1km(`_WALK_TRANSIT_THRESHOLD_M`)를 넘으면 대중교통(18km/h)/자차(42km/h) 속도로 전환된다 — "도보" 라벨이 붙은 옵션에는 안 맞는 가정이라, 1km 넘는 구간마다 실제 도보(4.5km/h)보다 훨씬 짧은 시간이 나갔음(예: 강남역-시청 8.7km가 약 46분(대중교통 속도)으로 표시 — 실제 도보로는 약 3시간). 거리 무관하게 항상 도보 속도로만 계산하는 `estimate_walk_minutes()`를 `travel_estimate.py`에 신규 추가하고 `get_walk_option()`이 이걸 쓰도록 교체. 테스트: `test_get_walk_option_uses_walk_speed_not_transit_speed_for_long_distance`, `travel_estimate.py` 자가검증(`python -m app.pipeline.travel_estimate`)에도 도보/대중교통 추정 비교 assertion 추가 |
| 활동 시간 고정 시 원래 순서를 유지하느라 다른 활동들을 전부 끌고 가던 문제 | ✅ | 2026-08-15 | 🔴 | 사용자 리포트: 하루 첫 활동을 15:30로 고정했더니 뒤 활동들이 전부 그만큼씩 밀려서 하루가 사실상 15:30부터 시작하는 것처럼 보임. `apply_manual_time()`은 원래 "고정한 활동의 원래 자리(순서)는 유지, 겹치는 이웃만 밀거나 당김" 방식이었는데, 순서를 유지하는 한 이 문제는 구조적으로 불가피(순서 유지 vs 시간순 재정렬 두 방향을 사용자에게 확인 — 시간순 재정렬 선택). `apply_manual_time()`을 고정할 활동을 일단 목록에서 뺀 뒤, 다른 활동들의 원래 시작 시각과 비교해 새 시간이 실제로 몇 번째에 해당하는지 찾아 그 자리에 끼워 넣고, 그 새 자리에서만 겹치는 이웃을 최소한으로 밀고/당기도록 재작성 — 안 겹치는 활동은 이제 아예 안 건드림. 마지막에 배열 순서대로 order 재배정. 겹치는 이웃이 이미 잠긴 경우 여전히 `ManualTimeConflictError`(409)로 막힘. 테스트: `test_activity_time_lock_repositions_instead_of_dragging_other_activities`, `travel_estimate.py` 자가검증에 재배치 시나리오 추가. 프런트는 부수적으로 발견된 문제도 같이 수정 — 시간 수정 모달에 종료 시각이 시작 시각보다 빨라도 서버 왕복 없이 즉시 막는 클라이언트 쪽 검증과 모달 내부 인라인 에러 표시 추가(기존엔 백엔드가 이미 422로 막고 있었지만, 에러 메시지가 모달에 가려진 페이지 상단 알림에만 떠서 사용자에게 안 보였음) |
| `Activity.is_custom` 추가 — 직접 검색해서 추가한 필수 장소를 일반 필수 장소와 구분 | ✅ | 2026-08-15 | 🟡 | 사용자 요청: "직접 추가한 항목은 이미지가 없는데(=일반 필수 장소와 같은 별 그림을 써서 구분이 안 됨), 하트·별처럼 다른 이미지를 만들어달라". `candidate_with_source_categories()`가 기존 `is_required`(전체 필수 장소 대상) 옆에 `is_custom`(그중 `ScheduleRequiredPlace.is_custom=True`인 것만)도 같이 채우도록 확장 — `is_required`가 이미 모든 응답 경로(GET /schedules/{id}, .../routes, 각종 preview/save, regenerate 등)에서 호출되는 공용 함수라 새 필드도 별도 배선 없이 전부 자동 적용됨. 프런트(`categoryImages.ts`)는 새 그림(돋보기, `custom.svg`)을 `isRequired`보다 먼저 검사하게 함(isCustom인 장소는 항상 isRequired도 true라서 순서가 중요). 테스트: `test_get_schedule_marks_custom_required_place_activity_as_is_custom` |
| `_candidate_reordered`가 손 안 댄 잠긴 활동의 잠금을 풀던 버그 근본 수정 | ✅ | 2026-08-17 | 🔴 | 사용자 리포트: 활동 하나를 잠근 뒤 다른 활동들의 순서만 바꿔도 잠긴 활동의 시간·잠금이 종종 풀림. 2026-08-14에 추가한 "실제로 자리를 옮긴 경우에만 잠금 해제"(위 항목 참고)의 판정식 `moved = original_order != new_order`가 원인 — 드래그로 항목 하나만 옮겨도 그 항목이 지나친 다른 모든 항목의 리스트 인덱스가 같이 밀리기 때문에, 손 안 댄 잠긴 활동도 인덱스가 바뀌면 "옮겨졌다"고 오판했다(4개 이상 항목에서 재현). `moved` 판정 자체를 제거하고 `time_locked=True`면 위치(order)가 바뀌어도 항상 시간·잠금을 보존하도록 단순화 — 잠긴 활동의 시간을 바꾸는 유일한 경로는 이제 명시적 "시간 잠금 해제" 뿐. 프런트도 재정렬 방식을 드래그에서 ▲▼ 버튼(한 번에 한 칸)으로 바꿔서 "어느 항목을 옮겼는지"의 모호함 자체를 없앰(프런트 `schedule.md` 참고). 테스트: `test_candidate_reordered_keeps_locked_activity_untouched_when_shifted_by_unrelated_drag`(회귀), `test_candidate_reordered_keeps_locked_activity_untouched_even_when_its_own_slot_moves`(기존 "실제로 옮기면 해제" 기대값을 뒤집음) |
| `_candidate_reordered`가 안 잠긴 활동을 잠긴 활동과 같은 시간대로 겹쳐 배치하던 버그 수정 | ✅ | 2026-08-17 | 🔴 | 사용자가 스크린샷으로 리포트: 재정렬 후 안 잠긴 활동 카드와 잠긴 활동 카드가 완전히 똑같은 시간대(13:09~14:39)로 보임. 원인은 바로 위 항목에서 "잠긴 활동은 위치가 바뀌어도 시간 그대로"로 고치면서도, 커서 기반 순차 배치가 아직 순서상 도달하지 않은(뒤쪽에 있는) 잠긴 활동의 시간대를 미리 알지 못해 그 위에 안 잠긴 활동을 그냥 얹어버렸던 것 — 원래 이 함수엔 겹침 검사 자체가 없었다(바로 위 항목의 ponytail 코멘트로 이미 예고돼 있던 부분). 모든 잠긴 활동의 시간대를 먼저 모아 정렬해두고, 안 잠긴 활동을 커서 위치에 놓으려 할 때마다 겹치는 잠긴 구간이 있으면 그 구간 뒤로 미는 방식으로 수정 — 잠긴 활동의 시간대는 이제 안 잠긴 활동이 못 들어가는 예약 구간으로 취급됨. 이 변경으로 기존 테스트 하나(`test_candidate_reordered_keeps_locked_activity_untouched_even_when_its_own_slot_moves`)의 기대값도 함께 바로잡음 — 그 테스트가 실은 같은 겹침 버그를 "정상"으로 잘못 assert하고 있었다. 테스트: `test_candidate_reordered_does_not_let_unlocked_activity_overlap_a_locked_one`(모든 활동 쌍이 안 겹치는지 전수 검사) |
