# 모두약속 — 프런트엔드 개발 계획

`docs/유저플로우_2026-08-05.md.md`, `docs/API명세서_2026-08-06.md` 기준. 우선순위는 `docs/기능명세서_2026-08-05.md`의 중요도를 따름.

**완료여부**: ✅ 완료 / ⬜ 예정 · **우선순위**: 🔴 높음 / 🟡 중간 / - 해당없음

| 개발 사항 | 완료여부 | 완료 일시 | 우선순위 | 비고 |
|---|---|---|---|---|
| 디자인 시스템 (doodle 컴포넌트 14종, `/kitchen-sink`) | ✅ | 2026-08-06 | 🟡 | `src/components/doodle/` |
| 전체 유저플로우 화면·라우팅 (목업 데이터 기반) | ✅ | 2026-08-06 | 🔴 | `stores/app.ts` 목업, 각 view에 연동 TODO 표시됨 |
| 전역 고정 로고·홈 이동 | ✅ | 2026-08-06 | 🟡 | `App.vue` |
| 백엔드 연결 상태 표시 (axios, `/health`) | ✅ | 2026-08-06 | - | `lib/api.ts` |
| Google 로그인 연동 (`POST /auth/google` 실연결) | ✅ | 2026-08-07 | 🔴 | Google Identity Services SDK(`index.html` 스크립트) + `LoginModal`에서 발급받은 id_token으로 실제 로그인. JWT는 백엔드가 `HttpOnly` 세션 쿠키로 발급하며, 앱은 `/me`로 로그인 상태를 복구 (2026-08-14 보안 전환) |
| API 키 등록 연동 (`POST`/`GET`/`DELETE /me/llm-credential`) | ✅ | 2026-08-12 | 🔴 | `ApiKeyEditView`/`ApiKeyView` 실제 API 호출로 교체, 제공자별 정규식 형식 검증 추가, 등록 상태 `localStorage` 유지. 입력 필드는 기본 `type="password"`로 마스킹, "보기" 토글로 확인 가능(2026-08-12 추가) |
| API 키 테스트 버튼 (`POST /me/llm-credential/test`) | ✅ | 2026-08-07 | 🟡 | `ApiKeyView`에 "키 테스트" 버튼 추가, 응답/실패 메시지 인라인 표시 |
| 지역 입력을 자연어 텍스트 → 시/도·세부지역 드롭다운으로 변경 | ✅ | 2026-08-07 | 🟡 | `ConditionWizardView`, 새 `DoodleSelect` 컴포넌트(옵션 패널까지 손그림 스타일 입힌 커스텀 리스트박스, 네이티브 select 아님) + `lib/regions.ts`. 세부지역 비우면 시/도 전체로 검색 |
| 지역 다중 입력 (최대 3개, 시/도만은 1개) + 포함관계 자동 정리 | ✅ | 2026-08-09 | 🟡 | `ConditionWizardView`. 같은 시/도 전체 선택 시 그 안의 세부지역 행 자동 제거. 백엔드 `NormalizedConditions.validate_regions()`가 같은 규칙 재검증 |
| 선호/비선호를 태그 선택 → 자유 텍스트로 변경 | ✅ | 2026-08-07 | 🔴 | `ConditionWizardView`, 각 100자 제한(`DoodleTextarea`의 `maxlength` prop). Step1 조건 정규화(LLM)가 태그를 추출하는 구조라 프런트는 원문 그대로 전달 |
| 예산 입력 1,000원 단위 step | ✅ | 2026-08-07 | - | `DoodleInput`에 `step` prop 추가, `ConditionWizardView` 예산 필드에 적용 |
| 일정 생성 연동 (`POST /schedules`) | ✅ | 2026-08-10 | 🔴 | `stores/app.ts`의 `buildMockCandidates`를 실제 API 호출로 교체. 위저드에 날짜 선택 UI가 없어 오늘 날짜(지났으면 내일)로 자동 보정(`buildTimeRange`, ponytail — 나중에 날짜 선택 UI 추가할 것). 409(조건 불만족)/그 외 오류를 `scheduleError`로 통일해 `CandidatesView`가 같은 알림으로 보여줌 |
| 일정 상세·동선 연동 (`POST /schedules/{id}/routes`) | ✅ | 2026-08-10 | 🟡 | `CandidateDetailView` 진입 시 자동으로 경로 조회, 구간마다 도보/대중교통/자차 옵션을 목록으로 보여주고 클릭으로 선택(`selectRouteOption` — 서버에 저장 안 함, 확정 전까지는 로컬 상태로만 유지). `info_needs_check`인 활동은 네이버 지도 링크로 자기확인 유도 |
| 후보 목록에서 필수 장소 선택·재생성 | ✅ | 2026-08-12 | 🔴 | `CandidatesView`의 좋아요/카테고리 장소마다 "일정에 추가하기" 제공. 선택한 장소는 좋아요 검색 결과 위의 강조 칩으로 영속 표시하고 ×로 해제 가능. "다시 일정 생성하기"는 선택 장소를 모두 포함한 후보를 새로 받으며, 실패하면 기존 후보를 유지한 채 이유를 표시 |
| 확정 연동 (`POST /schedules/{id}/confirm`) | ✅ | 2026-08-10 | 🟡 | `CandidateDetailView`의 "이 일정 확정하기" 버튼에서 호출 후 공유 화면으로 이동 |
| 공유 연동 (`GET /schedules/{id}`의 `share_slug`, `GET /share/{slug}`) | ✅ | 2026-08-10 | 🟡 | `ShareView`, `PublicShareView`. `POST /schedules/{id}/confirm` 응답의 `share_slug`를 그대로 쓰고(별도 "링크 생성" 엔드포인트 없음), `ShareView`는 새로고침 등으로 슬러그를 놓치면 `GET /schedules/{id}`를 다시 불러 복구(`fetchSchedule`, 세션이 메모리에 남아있을 때만). `createShareLink`(랜덤 slug 목업)는 제거 |
| 배포 설정 (Vercel) | ✅ | 2026-08-09 | - | `moduyaksok.vercel.app`. SPA 새로고침 404 방지용 `vercel.json` rewrite 추가 |
| 만남 목적 "기타" 옵션 추가 | ✅ | 2026-08-11 | 🟡 | `ConditionWizardView`의 `PURPOSES`에 date/friends/family/party 외 `other`("기타")가 빠져있던 걸 발견(백엔드 `purpose` Literal엔 이미 있었음) — 사용자가 지적해 추가 |
| 지역 목록에 서울 "용산" 추가 | ✅ | 2026-08-11 | - | `lib/regions.ts`의 `REGIONS.서울`에 누락돼 있던 걸 발견해 추가. 당시 백엔드 `app/services/regions.py`(광역 지역 자동 확장용)도 같은 목록으로 복제해뒀었는데, 그 파일은 이후 지역 범위가 단일 지역으로 축소되며 삭제됨(아래 "지역 입력을 다중 지역 → ..." 항목 참고) — 지금은 `lib/regions.ts`가 시/도·세부지역 선택 UI에만 쓰이는 유일한 목록 |
| 버그 수정 — 만료된 로그인 세션이 자동 정리 안 됨 | ✅ | 2026-08-11 | 🔴 | 로그인 후 access_token 만료(120분) 뒤 재방문하면 `store.loggedIn`이 계속 true로 남아 라우터 가드가 `/login`으로 안 돌려보내고, 모든 API가 조용히 401만 반복하는 좀비 상태가 됨(storage를 수동으로 지워야만 벗어날 수 있었음 — 사용자 리포트). `lib/api.ts`에 axios 응답 인터셉터 추가 — 401을 받으면 `store.logout()` + `/login`으로 강제 이동. 콘솔에 같이 뜨던 GSI "origin not allowed"/COOP 경고는 이 레포 코드(client_id는 프런트·백엔드 4개 env 파일 전부 동일값 확인, Google Console 승인된 origin도 정상) 문제가 아니라 그 브라우저 프로필에만 있던 상태(서비스워커/캐시 등, 시크릿 창에선 재현 안 됨) 쪽으로 추정 — 재발하면 계속 지켜볼 것. **2026-08-14 재발 확인**: 같은 "[GSI_LOGGER]: The given origin is not allowed for the given client ID" 콘솔 에러 재현(개발 세션을 오래 띄워둔 뒤) — 시크릿 창에서는 정상 로그인돼 브라우저 프로필 쪽 문제(쿠키/서비스워커/캐시)라는 추정이 다시 확인됨, 코드·Console 설정 문제 아님. 재현되면: DevTools Application 탭에서 Service Worker unregister + "Clear site data", 그래도 안 되면 `chrome://settings/content/all`에서 `localhost`/`accounts.google.com` 데이터 삭제. **실사용자 대응(같은 날, `LoginModal.vue`)**: 이런 브라우저 상태 문제는 배포 후에도 개별 사용자에게 재현될 수 있고 앱 코드로 원천 차단은 불가능하다고 판단 — 대신 GSI 버튼 렌더링 실패를 감지하는 안전망 추가. `renderButton()` 호출 3초 뒤 버튼 컨테이너가 비어있으면(렌더 실패로 판단) `lib/api.ts`의 401 인터셉터와 같은 `?login=1&redirect=...` 패턴으로 자동 새로고침 1회 시도(새로고침만으로 풀리는 경우가 실제로 있었음), `sessionStorage`(`gsi_button_render_retry`)로 재시도 여부를 새로고침 너머로 기억해 무한 새로고침 방지 — 그래도 실패하면 "광고 차단 확장 프로그램을 꺼거나 시크릿 창으로 시도해보세요" 안내 문구 표시. 로그인 성공 시 재시도 플래그 초기화 |
| 버그 수정 — 일정 후보 개수 안내 문구 하드코딩 | ✅ | 2026-08-11 | 🟡 | `CandidatesView`가 "일정 후보 3개를 만들었어요"를 고정 텍스트로 표시 — Step3가 검증 실패한 후보를 드롭해 3개 미만이 나올 수 있는데도(`synthesize_step3.py`, "후보 개수는 3개를 억지로 보장하지 않는다") 항상 3개라고 말해서 실제 카드 개수(2개)와 문구가 어긋남(사용자 발견). `store.candidates.length`로 동적으로 표시하도록 수정 |
| 지역 입력을 다중 지역 → 시/도+세부지역 단일 필수 선택으로 축소 | ✅ | 2026-08-11 | 🔴 | 백엔드가 여러 지역 조합 지원을 포기(`NormalizedConditions.regions: list[str]` → `region: str`, 네이버 지역검색 API의 `display`/`start` 제약상 지역을 쪼갤수록 결과가 희석되는 역효과 확인)한 데 맞춰 `ConditionWizardView`도 "지역 추가"/행 삭제/시·도만(전체) 선택 UI를 걷어내고 시/도 1개+세부지역 1개(둘 다 필수)만 받게 변경. `submitConditions` payload도 `regions: string[]` → `region: string` |
| 브라우저 탭 title/favicon을 "모두약속"으로 교체 | ✅ | 2026-08-14 | - | `index.html`의 `<title>`을 `frontend`(Vite 기본값 방치돼 있던 것)에서 `모두약속`으로 변경. favicon은 처음엔 원본 이미지 파일을 못 받아 디자인 시스템 손그림 스타일 SVG로 임시 대체했다가, 사용자가 `public/`에 실제 캐릭터 이미지를 넣어줘서 `public/favicon.png`로 교체하고 임시 SVG는 삭제 |
| 지역 목록 세분화 — 시/도별 세부지역 약 2배 확충 | ✅ | 2026-08-14 | 🟡 | `lib/regions.ts`의 `REGIONS` 각 시/도 세부지역을 압구정·청담·명동(서울), 의정부·파주·하남(경기) 등 실사용 빈도가 높은 동네 위주로 추가해 시/도당 항목 수를 대략 2배로 늘림. 백엔드 `naver_local_search.py`의 광역 시/도 자동 확장은 이 목록과 무관(별도 하드코딩 목록) — 필요시 같이 갱신할 것 |
| 일정 생성/재생성 진행 표시 (`DoodleProgress`) | ✅ | 2026-08-14 | 🟡 | 백엔드가 단계를 실시간 스트리밍하지 않아(`POST /schedules`는 단일 응답) 프런트가 이미 아는 조건(지역, 좋아요/싫어요 원문)만으로 "OO 맛집을 찾아보고 있어요" 등 문구를 만들어(`lib/progressMessages.ts`) 2.2초 간격으로 순환 표시 — 실제 백엔드 진행 단계와 정확히 동기화되진 않는 근사치. `ConditionWizardView`(최초 생성)와 `CandidatesView`(재생성) 양쪽에 적용. 백엔드 쪽 실제 단계별 로그는 `moduyaksok-backend/schedule.md` 참고 |
| URL에 세션(대화방) id 노출 — `/schedules/:sessionId[/candidates/:candidateId[/share]]` | ✅ | 2026-08-14 | 🔴 | 이전엔 `/schedules`, `/schedules/:id`(id가 사실은 candidate letter A/B/C)라 URL만 보고는 "어느 사용자의 어느 일정 세션"인지 특정할 수 없었음(사용자 발견 — 빈 브라우저에 URL만 붙여넣었을 때 재현이 안 됨) — 항상 프런트 메모리·localStorage에 남은 마지막 세션에 의존했음. `router/index.ts`를 `/schedules/:sessionId?` → `/schedules/:sessionId/candidates/:candidateId` → `.../share` 3단 중첩으로 재구성. `CandidatesView`/`CandidateDetailView`/`ShareView`가 이제 `route.params.sessionId`로 `GET /schedules/{id}`(`store.fetchSchedule`)를 직접 호출 — 로그인 세션은 있는데 다른 사용자 소유면 백엔드 `_get_owned_session`이 403을 주므로 URL을 안다고 남의 세션을 볼 수는 없음. sessionId 없는 `/schedules`(하위호환)는 `restoreDraftSchedule()`로 최근 draft를 찾은 뒤 정식 URL로 `router.replace` |
| 로그인을 별도 페이지 대신 메인 화면 위 모달로 전환 | ✅ | 2026-08-14 | 🟡 | 비로그인 상태로 "일정 만들기 시작"/"설정" 등 보호된 라우트에 들어가면 `/login` 페이지로 이동하던 걸, 메인 화면(`/`)으로 보내고 그 위에 `LoginModal`(`components/doodle/LoginModal.vue`)을 띄우도록 변경(사용자 요청). `router.beforeEach` 가드가 `stores/auth.ts`의 `openLoginModal(to.fullPath)`를 부르고 `/`로 리다이렉트, 로그인 성공 시 원래 경로로 이어서 이동. `lib/api.ts`의 401 인터셉터(전체 새로고침이라 Pinia 상태가 날아감)는 `?login=1&redirect=...` 쿼리로 의도를 넘기고 `App.vue`가 마운트 시 다시 읽어 모달을 연다. `LoginView.vue`/`/login` 라우트는 삭제 |
| 로그아웃 버튼을 빨간색으로 | ✅ | 2026-08-14 | - | `SettingsView`의 로그아웃 버튼을 `variant="ghost"`(잉크색 외곽선)에서 `variant="primary"`(빨간 배경)로 변경 |
| 후보 목록 UI 다듬기 — 지도 보기를 버튼으로, 추가하기 버튼 폭 축소 | ✅ | 2026-08-14 | 🟡 | `CandidatesView`의 "지도 보기"가 밑줄 텍스트 링크라 하이퍼링크처럼 보인다는 지적(사용자) — 테두리 있는 작은 버튼 스타일로 변경(`<a>` 태그는 유지, 새 탭으로 여는 외부 링크라 시맨틱상 버튼 컴포넌트로 바꾸진 않음). "일정에 추가하기" 버튼 폭도 `w-32`→`w-20`으로 축소 |
| 백엔드 헬스체크 표시(우측 하단 빨간 점) 제거 | ✅ | 2026-08-14 | - | `App.vue`가 `GET /health`로 연결 상태를 우측 하단 점으로 표시하던 초기 스캐폴딩(2026-08-06)이 정상 연결일 때 오히려 `bg-red`로 떠서 디자인상 어색하다는 지적(사용자) — 인디케이터와 그 밑의 `/health` 폴링 자체를 제거 |
| "확정된 일정" → "나의 일정"으로 개편, 확정 전 draft도 함께 표시 | ✅ | 2026-08-14 | 🔴 | 사용자 리포트: 확정하기 전 일정이 "확정 일정" 목록에 안 보임 — 실제로는 이미 draft로 저장돼 있는데(이전 항목 "미확정 일정 저장" 참고) 이 목록만 `status="confirmed"` 필터링을 걸고 있었음. 백엔드 `GET /confirmed-schedules`가 draft도 함께 반환하도록 변경(백엔드 `schedule.md` 참고)에 맞춰 `ConfirmedSchedulesView`도 `DoodleBadge`로 초안/확정 상태를 구분 표시하고, 카드 클릭 시 확정 일정은 공유 화면으로·초안은 이어서 만들던 화면(`/schedules/:id`)으로 분기. 이름 수정은 백엔드가 확정 일정에만 허용해서(`PATCH .../title` 여전히 409) 초안 카드에서는 버튼을 숨김 — 삭제는 아래 항목대로 초안도 가능해짐. 네비게이션 라벨("확정 일정" → "나의 일정")·페이지 제목·부제·빈 상태 문구도 함께 변경. API 경로(`/confirmed-schedules`)와 라우트 경로는 그대로 유지 — 이름과 실제 동작이 안 맞지만 호출부 전부를 도미노로 바꾸는 범위라 이번엔 안 건드림 |
| 초안 일정도 삭제 가능하게 | ✅ | 2026-08-14 | 🟡 | 바로 위 개편 직후 사용자가 "초안에는 왜 삭제 버튼이 없냐"고 바로 지적 — 백엔드 `DELETE /schedules/{id}`의 confirmed 전용 제약을 없앤 것(백엔드 `schedule.md` 참고)에 맞춰, `ConfirmedSchedulesView`의 "⋯" 메뉴(삭제)를 확정 일정 전용에서 모든 카드로 되돌림. 삭제 확인 문구도 초안엔 "공유 링크"가 없다는 걸 반영해 상태별로 다르게 표시(`"...일정과 대화방, 공유 링크를..."` vs `"...일정과 대화방을..."`) |
| `CandidateDetailView` 장소 카드 드래그 순서 변경 | ✅ | 2026-08-14 | 🟡 | 사용자 질문을 계기로 설계·구현 — 드롭 즉시(500ms 디바운스 후) `previewCandidateReorder()`로 교통편/지도 재계산 미리보기, "저장" 눌러야 확정(기존 "이 장소 빼기" preview/save 패턴 재사용). 카드 목록은 `draggableActivities`(로컬 배열)가 정본이라 드롭 즉시 화면에 반영되고, 저장 없이 연속으로 드래그 가능 — 응답이 겹치면 시퀀스 번호로 가장 나중에 보낸 요청만 반영(`reorderRequestSeq`). 드래그는 `vue-draggable-plus`(SortableJS 래퍼, 터치 지원) 신규 의존성, `handle=".drag-handle"`로 카드 안 "≡" 아이콘에서만 드래그 시작. 다른 미리보기(대체/제외) 진행 중이거나 저장 중엔 드래그 비활성화(`dragDisabled`). 설계 문서: `docs/superpowers/specs/2026-08-14-candidate-reorder-transport-design.md`. 버그 수정(같은 날): 사용자 리포트로 드래그가 가끔 원위치로 되돌아가는 문제 발견 — 기본값(네이티브 HTML5 drag-and-drop)에서는 SortableJS가 드롭 위치의 `newIndex`가 `oldIndex`와 같다고 판단하면(대상 항목을 완전히 넘기지 못한 드래그) DOM을 원래 스냅샷으로 강제 복원하는 내부 로직이 있음 — `:force-fallback="true"`(+`:fallback-tolerance="3"`)로 라이브러리의 JS 기반 드래그로 전환해 해결(SortableJS 계열에서 이 증상의 표준 해결법, 터치 안정성도 같이 개선) |
